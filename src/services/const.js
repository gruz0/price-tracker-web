const path = require('path')

export const usersPath = path.join(process.cwd(), '/data/users')
export const productsPath = path.join(process.cwd(), '/data/products')
export const productsQueuePath = path.join(process.cwd(), '/data/queue')
